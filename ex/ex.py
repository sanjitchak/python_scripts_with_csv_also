#!/usr/bin/python3
import re,random,os

def grammer_regex(filename):
                 noun_regex = re.compile(r'NOUN')

                 adjective_regex = re.compile(r'ADJECTIVE')
                 verb_regex = re.compile(r'VERB')
                 adverb_regex = re.compile(r'ADVERB')

                 if !os.path.isfile('./filename'):
                                              return
                 new_file= open('./filename')
                 list_lines= new_file.readlines
                 for lines in list_lines:
                     if noun_regex.findall(lines):
                         for i in nou


for filename in os.listdir('.'):


